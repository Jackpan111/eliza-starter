{
  "name": "@elizaos/eliza-starter",
  "version": "0.1.9",
  "scripts": {
    // Buduje tylko chat-server: kod + deklaracje .d.ts
    "build:chat": "tsup src/chat-server.ts --format esm --dts",

    // Domyślny entrypoint: najpierw build:chat, potem uruchomienie
    "start": "pnpm run build:chat && node dist/chat-server.js --non-interactive"
  },
  "dependencies": {
    /* … */
  },
  "devDependencies": {
    "tsup": "^8.x",
    "typescript": "^5.x",
    /* … */
  }
}
